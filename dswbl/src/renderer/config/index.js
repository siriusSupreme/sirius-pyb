// const NODE_ENV = process.env.NODE_ENV
// const OPTIONS = NODE_ENV === 'production' ? require('./production.js') : require('./renderer.js')
// const BL_CONFIG = OPTIONS.NAMESPACE || 'bl-config'
// const SYMBOL = BL_CONFIG
// let G_CONFIG = window[SYMBOL] || {}
// window[SYMBOL] = G_CONFIG = Object.assign(window.Object.create(null), OPTIONS, G_CONFIG)
// module.exports = G_CONFIG
